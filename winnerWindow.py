import PySimpleGUI as sg

#at the moment this only shows before the current window: doesn't appear on top
#def winnerWindowAppear():
   # winner = True
   # while winner:
   # ok = sg.popup_ok('You have won x',modal = True)
   # if ok=="OK":
   #     break